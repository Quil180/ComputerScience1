#define FULL 5
#define EMPTY -9999

int main() {
  // basic queue operations (and this is the only way to look at the queue)
  // enqueue adds a new item at the end of the queue
  // dequeue gets and removes the item from the front of the queue
  // isempty checks if the queue is empty
  // isfull check if the queue is full
  // peek gets the item at the front without deleting the item from the queue

  // there are 2 queue types: linear and circular
  // you always delete from front (first in, first out)

  return 0;
}
